---
deployment:
  tasks:
    - export DEPLOYPATH=/home/edara/public_html/
    - /bin/cp -R * $DEPLOYPATH
    - /bin/cp -R .env $DEPLOYPATH
    - /bin/cp -R vendor $DEPLOYPATH
    - /bin/cp -R storage $DEPLOYPATH
